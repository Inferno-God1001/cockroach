from setuptools import setup, find_packages

setup(
    name="cockroach",
    version="1.0.0",
    description="Simple",
    author="Inferno-God1001",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.8"
)